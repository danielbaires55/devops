/**
 * Funzione per calcolare la differenza tra due numeri
 * 
 * @param {number} a - Primo numero
 * @param {number} b - Secondo numero
 * @returns {number} - Differenza tra `a` e `b`
 */
export function calculateDifference(a, b) {
    return a - b;
}